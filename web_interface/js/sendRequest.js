function sendRequest(displayed_docs,clicked_docs,search_keyword) {

	alert(displayed_docs);

	// if ($user_eval != -1)
	// 	$sql = "INSERT INTO action (COMP_ID, TIMESTAMP, ACTION, CONTENT) VALUES ('$ir_uid', now(), '$type', '$content')";

} 